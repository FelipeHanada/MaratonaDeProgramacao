#include <bits/stdc++.h>
using namespace std;


void solve() {
    int n, m;
    cin >> n >> m;
    vector adj(n, vector<int>());
    for (int i=0; i<m; i++) {
        int u, v;
        cin >> u >> v;
        u--; v--;
        // adj[u];
    }
}


int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
}