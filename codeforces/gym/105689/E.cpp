#include <bits/stdc++.h>
using namespace std;


int s[2];
int m[2];

string solve() {

}

int main() {
    int q;
    cin >> q;

    for (int i=0; i<q; i++) {
        cin >> s[0] >> s[1] >> m[0] >> m[1];
        cout << solve() << "\n";
    }
}