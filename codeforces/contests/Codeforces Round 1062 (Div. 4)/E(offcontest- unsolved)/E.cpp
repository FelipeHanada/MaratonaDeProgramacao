#include <bits/stdc++.h>
using namespace std;


void solve() {
    int n, k, x;
    cin >> n >> k >> x;
    deque<int> a(n);
    for (int i=0; i<n; i++) {
        cin >> a[i];
    }

    deque<int> b(n + 1);
    
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
}
