#include <bits/stdc++.h>
using namespace std;

#define ll long long

string solve(ll n) {
    ll n5 = 5 * n;
    // 1-check: 5*n + 4 is a perfect square
    if (round(sqrt(n5 + 4)) 

    // 2-check: 5*n - 4 is a perfect square
}

int main() {
    int t;
    cin >> t;

    ll n;
    for (int i=0; i<t; i++) {
        cin >> n;
    }
}