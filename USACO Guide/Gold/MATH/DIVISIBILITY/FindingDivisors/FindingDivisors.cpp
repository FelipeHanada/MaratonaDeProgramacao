#include <bits/stdc++.h>
using namespace std;


vector<int> divisors(int n) {

}


int main() {
    int n; cin >> n;

    vector<int> ans = divisors(n);
    for (int x : ans) {
        cout << x << endl;
    }
}
